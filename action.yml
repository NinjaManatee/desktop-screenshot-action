name: "Create a desktop screenshot"
description: "Create a screenshot of the runner's desktop"
inputs:
    file-name:
        description: "Name of screenshot file"
        default: "screenshot.jpg"
runs:
    using: "node22"
    main: "index.js"
